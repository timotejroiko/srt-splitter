; Global opt-out: dependency install scripts never run automatically.
; Windows needs this (upstream vcpkg + VS2019 script is broken there).
; POSIX keeps working via `npm run setup`, which runs the binding's own
; ./configure + make + rebuild explicitly (see the !isWin branch).
ignore-scripts=true
